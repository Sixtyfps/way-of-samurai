import React from "react";

type HeaderType = {

}

export const Header: React.FC<HeaderType> = (props) => {
    return (
        <header className='header'>
            <img src="#" alt="image"/>
        </header>
    )
}